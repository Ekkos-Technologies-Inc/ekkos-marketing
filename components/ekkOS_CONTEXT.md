---
system_id: apps-marketing-components
domain: platform
status: active
last_compiled_at: 2026-03-10T20:17:25.466Z
content_hash: 789c905f1ad5b8f31d77a1afb69bbe5aa894cd8f70eaa73ad2107aafa00ad5c6
invalidation_fingerprint: 9cb598a95ca0cd188ea0c616824ca6421ec01270c20d16ddb53d8af2bbc3e0cf
recompile_reason: "5 git commits, 47 graph edges"
source_event_ids: []
source_event_count: 5
evidence_window_start: 2026-02-12T17:14:12-05:00
evidence_window_end: 2026-03-09T20:51:11-04:00
confidence: medium
redactions: 0
compiled_by: ekkOS Cortex
compiler_model: gemini-3-flash-preview
---

<!-- EKKOS_AUTOGENERATED_START 789c905f1ad5b8f31d77a1afb69bbe5aa894cd8f70eaa73ad2107aafa00ad5c6 -->

# apps/marketing/components

This directory serves as the primary UI component library for the ekkOS marketing platform. It contains high-fidelity React components ranging from foundational UI elements (modals, buttons) to complex 3D backgrounds and interactive marketing sections used to demonstrate the platform's capabilities.

## Architecture
- **Runtime**: React with TypeScript (TSX). The components are designed for client-side rendering, utilizing modern browser APIs for animations and 3D effects.
- **Entry points**: 
    - `Providers.tsx`: The root context provider that likely wraps the marketing application to provide theme, state, or third-party library initialization.
    - `marketing/Header.tsx` & `marketing/Footer.tsx`: The structural bookends for all marketing pages.
    - `transitions/PageTransition.tsx`: Manages the entry/exit animations for top-level route changes.
- **Key dependencies**: 
    - **Framer Motion**: Inferred from components like `ScrollReveal.tsx`, `AnimatedCounter.tsx`, and `PageTransition.tsx`.
    - **Three.js / Canvas**: Likely used for `background/ParticleField.tsx` and `marketing/PulseHero3D.tsx`.
    - **Tailwind CSS**: Standard styling utility for the `ui/` and `marketing/` subdirectories.
    - **Lucide React**: Common icon set used in `ui/` components (inferred from standard ekkOS patterns).
- **Deploy target**: Vercel or similar frontend hosting, as part of the `apps/marketing` Next.js/React application.

## Key Files
- `Providers.tsx`: The central wrapper for the marketing site; manages global state and context providers.
- `marketing/InteractiveDemo.tsx`: A high-complexity component used to showcase the ekkOS platform features in a live-simulated environment.
- `marketing/ArchitectureDiagram.tsx`: A specialized component for rendering the technical stack of ekkOS, likely using SVG or Canvas.
- `background/ParticleField.tsx`: A performance-optimized background animation that provides the "tech" aesthetic for the landing pages.
- `marketing/PulseHero3D.tsx`: The primary visual hook for the homepage, likely involving 3D geometry or heavy shader work.
- `ui/ShimmerButton.tsx`: A reusable, high-conversion Call-To-Action (CTA) component with advanced CSS/animation effects.
- `marketing/IntelligenceTrace.tsx`: Visualizes data flow or AI processing steps, critical for explaining the "platform" domain.
- `ui/MetricCard.tsx`: Standardized display for statistics; note that a marketing-specific version also exists in `marketing/MetricCard.tsx`.

## API / Exports
The directory exports a wide array of React components categorized by their visual role:

### Backgrounds (`background/`)
- `ParticleField`: Renders a dynamic particle system.
- `SwarmBackground` & `SwarmBackgroundLoader`: A specialized "swarm" animation, likely used for AI-themed sections.
- `AuroraBackground`: A flowing, gradient-based light effect.

### Core UI (`ui/`)
- `EkkosLogo`: The official SVG branding component.
- `Modal` & `Toast`: Standard overlay and notification systems.
- `MetricCard` & `AnimatedCounter`: Components for displaying data points with entry animations.
- `GlitchText` & `ShimmerButton`: Stylized text and button components for high-impact sections.
- `GlassCard`: A container component implementing glassmorphism (blur/transparency).
- `EarlyAccessBanner`: A sticky or header-top component for lead generation.

### Marketing Sections (`marketing/`)
- `AstronautHero`: A themed hero section.
- `NvidiaPartnershipCard`: A specific component highlighting the Nvidia ecosystem connection.
- `GrowthTimeline` & `LearningCurve`: Data visualization components for roadmap and ease-of-use metrics.
- `ComparisonCard`: A pricing or feature-parity table component.
- `LayerList`: Used to describe the ekkOS stack layers.

### Effects & Transitions
- `effects/CursorGlow.tsx`: An interactive mouse-following glow effect.
- `transitions/PageTransition.tsx`: Wrapper for smooth route transitions.

## Environment Variables
While no `.env` files are in this specific directory, the components likely consume:
- `NEXT_PUBLIC_APP_URL`: For links between marketing and the main web app.
- `NEXT_PUBLIC_GA_ID`: If `Providers.tsx` initializes Google Analytics.

## Conventions
- **Categorization**: Components are strictly grouped by intent. `ui/` contains primitives, while `marketing/` contains composite sections specific to the marketing site's narrative.
- **Visual Polish**: Extensive use of "glow," "shimmer," "pulse," and "glitch" effects indicates a high-tech, futuristic design language.
- **Naming**: Files use PascalCase (e.g., `ExampleCard.tsx`).
- **Performance**: Background components (like `SwarmBackgroundLoader`) suggest that heavy assets are loaded asynchronously to protect PageSpeed scores.
- **Duplication Note**: There are two `MetricCard.tsx` files (one in `ui/`, one in `marketing/`). The `marketing/` version is likely a more complex, styled variant of the base `ui/` primitive.

## Recent Changes
- **2026-03-09**: Fixed marketing statistics components, performed a migration to Gemini (likely affecting AI-driven text or demo components), and updated the ekkOS context documentation.
- **2026-02-26**: System restoration event; files were restored to a previous stable state.
- **2026-02-16**: Documentation update across the system reduced the tool count from 68 to 32, which may have affected the `ArchitectureDiagram` or `FeatureBlock` content.

## Gotchas
- **Z-Index Management**: With multiple background effects (`ParticleField`, `AuroraBackground`) and overlays (`Modal`, `Toast`), z-index collisions are a potential risk.
- **GPU Load**: `PulseHero3D` and `ParticleField` may cause performance degradation on lower-end mobile devices if not properly throttled or disabled via media queries.
- **Hydration**: Components using `CursorGlow` or `ScrollReveal` must handle React hydration carefully to avoid mismatch errors between server-rendered HTML and client-side animation states.
- **Restoration State**: The "Restore" event on 2026-02-26 suggests the codebase may have undergone significant churn recently; verify component props against the latest `apps-marketing` implementation.

## Connected Systems
- **Direct Dependents**: 
    - `apps-marketing`: The primary consumer of these components.
- **Co-change Coupling**: 
    - `apps-web-app` & `apps-web-components`: Often updated alongside this directory to maintain visual parity between the marketing site and the actual platform.
    - `apps-marketing-hooks`: Provides the logic (e.g., scroll position, form handling) that drives these components.
    - `extensions-ekkos-connect`: Shared branding or UI logic often syncs with marketing updates.
    - `supabase`: Likely used for the `EarlyAccessBanner` or newsletter signups within the `marketing/` components.

<!-- EKKOS_AUTOGENERATED_END -->

---
*Generated from observed work patterns, decisions, corrections, and file activity.*
